<% @order.tickets.each do |ticket| %>

  <%= render 'shared/ticket_pdf',
    ticket: ticket,
    event_category: ticket.event_category,
    event: @order.event,
    order: @order
  %>

<% end %>
<br>
