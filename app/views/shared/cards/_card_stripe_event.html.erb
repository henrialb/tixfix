<div class="card-stripe-event">
  <%= link_to '', event, class: 'card-stripe-event-link' %>
  <div class="d-flex justify-content-between">
    <% if event.image.attached? %>
      <%= cl_image_tag event.image.key, height: 300, width: 400, crop: :fill, alt: "flyer" %>
    <% else %>
      <%= image_tag 'placeholder.jpg' %>
    <% end %>
    <div class="card-stripe-event-content flex-grow-1">
      <h2 ><%= link_to event.name, event %></h2>
      <p><%= event.venue.name %></p>
      <p><%= event.starts_at.strftime("%a %d %b %Y, %-l:%M%p") %></p>
    </div>
    <div class="card-stripe-event-date d-flex flex-column justify-content-between">
      <div>
      </div>
      <p><%= relative_time(event) %></p>
      <h3><%= number_to_currency(total_revenue(event), unit: '€', delimiter: ',', precision: 0) %>💰</h3>
    </div>
  </div>
</div>
